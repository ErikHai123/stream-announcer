# Скрипт использует только стандартную библиотеку Python (urllib, json, zoneinfo),
# поэтому внешних зависимостей не требуется.
# Файл оставлен на случай, если в будущем понадобятся дополнительные пакеты.
